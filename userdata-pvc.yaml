apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: userdata-claim
  namespace: default
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 8Gi
  storageClassName: manual
