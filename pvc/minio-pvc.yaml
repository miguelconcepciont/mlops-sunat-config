apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minio-claim
  namespace: default
spec:
  volumeName: minio-pv
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 8Gi
  storageClassName: nfs-storage
