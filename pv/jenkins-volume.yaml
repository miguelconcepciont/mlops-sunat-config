apiVersion: v1
kind: PersistentVolume
metadata:
  name: jenkins-pv
  namespace: default
  labels:
    name: jenkins-pv
spec:
  storageClassName: nfs-storage
  accessModes:
  - ReadWriteMany
  capacity:
    storage: ${volumeSize}
  persistentVolumeReclaimPolicy: Retain
  nfs:
    server: ${nfsServer}
    path: ${jenkinsHomePath}